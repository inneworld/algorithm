### 2022 0722

오늘은 백준 문제를 풀면서 몇가지를 깨달았다.
```
먼저 sorted()함수를 쓸때 
sorted(array,key = int)
이런식으로 원하는 자료형으로 정렬을 할 수 있는데,
이 내용에 대해서 알고 있었지만 이걸 왜 구분해야 하는지에 대해 알지 못했는데,
이런 내용이었다.

어떠한 배열에서 인자값을 정렬할때 , str식으로 할때랑 int식으로 할때랑 정렬 되는 것이 달랐다.
나는 숫자는 당연히 똑같을줄 알았는데 , 좀 안일했던 것 같다.

그리고 또 하나 배운 것은
list는 list자료형이기 때문에 + 연산이나 - 연산 등을 해주기 위해서는 형변환이 필요한데,
굳이 여러줄 쓸필요 없이
array = [int(i) for i in array]
이런식으로 해주면 된다.
```