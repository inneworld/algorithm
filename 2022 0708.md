### 2022 0708
```
오늘은 python에 대해서 공부했다. 원레 c언어만 쓰면서 python은 눈치것 했었는데 그냥 배우기로 했다.

변수이름에 숫자가 먼저 들어가거나 띄어쓰기 특수문자 안된다.
함수종류 
print , input(안에 글자넣으면 그 글자 출력되고 입력받음) , len등등 이용 가능 len함수에 리스트를 넣으면 원소 갯수를 알려줌
max 함수는 리스트에 최댓값을 찾아줌
min은 리스트에  최솟값을 찾아줌
sorted()는 리스트를 오름차순으로 정렬해줌 sorted([1,2,3,4],reverse=True)이런식으로 하면 내림차순 정렬
type함수는 무슨 자료형인지를 알려줌.
형변환은 int("1234")이런식으로 가능

사용자 정의 함수 만드는 법
def 이름(매개변수)
들여쓰기로 구분

인터프리터는 원래 한줄씩 번역을 하지만 함수를 만나면 스킵한다. 그리고 c언어로 따지면 main함수부터 처리한다.

자료형의 구분

------문자열

"" 혹은 '' 혹은 """ """ 혹은 ''' '''안에 들어있으면 문자열로 인식
문자열의 덧셈을 지원함 foo = "Hello" + "World" 
곱샘도 지원함 대신 문자열 2개는 안되고 문자열과 숫자로만 됨 foo = "Hell" * 3

변수 이름.count("a") d이런식으로 하면 a가 문자열에 몇개 들어있는지 확인
변수 이름.upper()이렇게 하면 모두 대문자로
변수이름 .lower()이렇게 하면 전부 소문자로
변수이름.strip()이렇게 하면 공백 없애줌
변수이름.replace("문자열","대체문자열)이런식으로 특정단어를 바꿀 수 있음
변수이름.splic(",") 이런식으로 하면 , 를 기준으로 모든 문자열을 나눈다.

리스트에 값이 들어있으면
리스트이름[0:5] 이런식으로 6개를 출력할수 도 있음
문자열 합쳐서 출력하기
print("Hello {} ".format(변수이름))
print("Hello %s"%a)이런식으로 서식지정자도 이용
print(f"Hello {})" 이런식으로 앞에 f를 해서 사용가능

------- 정수형

연산자 가 +, *, -, /, **(제곱), ** (1/2) 이런식으로 루트도 가능, % , // (나눈 몫만 알려줌)

-------리스트 형
배열 같은거
배열을 그냥 출력가능하다
hero = [원소들]
이런식으로 있는데 print(리스트이름) 이렇게 출력 가능
리스트 이름.append(원소값 혹은 [번호]) 하면 원소에 추가 가능
리스트 이름.remove(원소값 혹은 [번호]) 하면 원소를 삭제 가능
리스트 안에 리스트가 있으면 2차원 배열처럼 사용 가능 print([3][2]) 이런식으로 ㄱㄴ
리스트 2개를 더하게 되면 첫번째 리스트 원소 뒤에 두번째 리스트 원소가 들어감
리스트도 곱셈 가능

---------딕셔너리
key 와 value를 설정 가능
fruit = {"사과":1000,"포도":2000,:"오렌지":3000}
이런식으로 원소에 이름(key)을 지어줄 수 있음
print(fruit["사과"]) 이런식으로 출력 가능
fruit["포도"] = 1800 이런식으로 수정 가능
값을 삭제 하고 싶다면 del fruit["포도"] 이런식으로 삭제 가능
딕셔너리이름.keys()이렇게 하면 딕셔너리 이름만 출력 가능
딕셔너리 이름.values()이렇게 하면 딕셔너리 value만 출력 가능
딕셔너리 이름.items 이렇게 하면 key와 value를 출력 가능
